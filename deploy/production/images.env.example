# Copy to .env on each production host.
# Keep all three references immutable for deterministic rollback.
PLYWO_IMAGE_TAG=sha-<full-git-sha>
PLYWO_CLOUDFLARED_IMAGE=cloudflare/cloudflared@sha256:<digest>
# Executor host only:
PLYWO_POSTGRES_IMAGE=postgres@sha256:<digest>
