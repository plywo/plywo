name: Isolated Compose Provider Proof

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  isolated_compose_provider:
    runs-on: ubuntu-latest

    env:
      PLYWO_EXECUTOR_IMAGE: plywo-isolated-executor:${{ github.sha }}
      PLYWO_PROVIDER_IMAGE: plywo-compose-provider:${{ github.sha }}
      PLYWO_PROVIDER_VOLUME: plywo-provider-${{ github.run_id }}-${{ github.run_attempt }}
      PLYWO_COMPOSE_PROVIDER_SOCKET: /run/plywo-provider/provider.sock

    steps:
      - uses: actions/checkout@v4

      - name: Build executor and isolated provider images
        run: |
          docker build -t "$PLYWO_EXECUTOR_IMAGE" .
          docker build -f Dockerfile.compose-provider -t "$PLYWO_PROVIDER_IMAGE" .

      - name: Start isolated provider authority
        run: |
          docker volume create "$PLYWO_PROVIDER_VOLUME"
          docker run -d --name plywo-compose-provider \
            -v /var/run/docker.sock:/var/run/docker.sock \
            -v "$PLYWO_PROVIDER_VOLUME:/run/plywo-provider" \
            -e PLYWO_COMPOSE_PROVIDER_SOCKET="$PLYWO_COMPOSE_PROVIDER_SOCKET" \
            "$PLYWO_PROVIDER_IMAGE"

          for attempt in $(seq 1 60); do
            if docker exec plywo-compose-provider test -S "$PLYWO_COMPOSE_PROVIDER_SOCKET"; then
              break
            fi
            if [ "$attempt" -eq 60 ]; then
              docker logs plywo-compose-provider
              exit 1
            fi
            sleep 1
          done

      - name: Verify executor has no container-management authority
        run: |
          docker run --rm \
            -v "$PLYWO_PROVIDER_VOLUME:/run/plywo-provider" \
            "$PLYWO_EXECUTOR_IMAGE" \
            sh -c 'test ! -S /var/run/docker.sock && ! command -v docker'

      - name: Verify customer UID cannot access provider control socket
        run: |
          docker run --rm --user 10001:10001 \
            -v "$PLYWO_PROVIDER_VOLUME:/run/plywo-provider" \
            -e PLYWO_COMPOSE_PROVIDER_SOCKET="$PLYWO_COMPOSE_PROVIDER_SOCKET" \
            "$PLYWO_EXECUTOR_IMAGE" \
            ruby -rsocket -e '
              begin
                UNIXSocket.new(ENV.fetch("PLYWO_COMPOSE_PROVIDER_SOCKET"))
                abort "subject UID unexpectedly opened provider control socket"
              rescue Errno::EACCES
                puts "subject_provider_authority_access=false"
              end
            '

      - name: Prove typed Compose lifecycle through isolated provider
        run: |
          docker run --rm --network host \
            -v "$PLYWO_PROVIDER_VOLUME:/run/plywo-provider" \
            -e PLYWO_COMPOSE_PROVIDER_SOCKET="$PLYWO_COMPOSE_PROVIDER_SOCKET" \
            "$PLYWO_EXECUTOR_IMAGE" \
            ruby script/prove_isolated_compose_provider.rb

      - name: Verify provider resources were removed
        run: |
          if docker ps -a --filter label=com.docker.compose.project --format '{{.Names}}' | grep '^plywo-' ; then
            echo "Compose containers leaked after proof"
            docker ps -a
            exit 1
          fi
          if docker network ls --filter label=com.docker.compose.project --format '{{.Name}}' | grep '^plywo-' ; then
            echo "Compose networks leaked after proof"
            docker network ls
            exit 1
          fi

      - name: Provider logs
        if: always()
        run: docker logs plywo-compose-provider || true

      - name: Teardown provider authority
        if: always()
        run: |
          docker rm -f plywo-compose-provider || true
          docker volume rm -f "$PLYWO_PROVIDER_VOLUME" || true
